DATABASE_URL="postgresql://robecop:robecop_pass@localhost:5432/robecop_dev?schema=public"
JWT_SECRET=change_this_secret
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=robecop@example.com
PORT=4000
AUTH_MODE=jwt
# Optional Redis URL for production rate-limiter store
# Example: redis://:password@redis-host:6379
REDIS_URL=
ACCESS_TOKEN_EXPIRES_MINUTES=15
REFRESH_TOKEN_EXPIRES_DAYS=7
# Cookie settings when USE_COOKIES=true
COOKIE_SAMESITE=lax
COOKIE_SECURE=false
FROM_EMAIL=robecop@example.com
PORT=4000
AUTH_MODE=jwt
# Microsoft Entra ID / Azure AD (for SSO)
AUTH_MODE=JWT # or ENTRA
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=http://localhost:4000/auth/entra/callback
FRONTEND_URL=http://localhost:3000

# Comma-separated allowed origins for CORS (default: http://localhost:3000)
ALLOWED_ORIGINS=http://localhost:3000

# If true, backend will set an HttpOnly cookie `robecop_token` on login and enable CSRF protection
# USE_COOKIES=false

# Auth mode: 'jwt' for local dev, 'entra' to enable Microsoft Entra ID SSO
AUTH_MODE=jwt

# Microsoft Entra ID (Azure AD) settings — required if AUTH_MODE=entra
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=http://localhost:4000/auth/entra/callback

# Frontend URL used for redirect after login
FRONTEND_URL=http://localhost:3000

# Comma separated lists to map emails to roles (optional)
AZURE_ADMIN_EMAILS=admin@example.com
AZURE_SUPERVISOR_EMAILS=
